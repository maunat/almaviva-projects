package it.almaviva;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeMeasureResourceIT extends MeasureResourceTest {

    // Execute the same tests but in native mode.
}
